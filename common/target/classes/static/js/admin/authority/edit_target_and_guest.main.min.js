import Edit from './admin_target_and_guest_authority.js';
import Tree from './admin_target_and_guest_tree_selected.js';

window.editTargetAndGuestInit = function($, opt){
  Tree($, opt);
  window.editTargetAndGuestJs = new Edit($, opt);
}